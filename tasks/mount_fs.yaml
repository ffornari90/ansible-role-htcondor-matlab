- name: Create NFS mountpoint
  become: yes
  file:
    path: /data
    state: directory
  when: is_not_master_host

- name: Mount NFS volume
  become: yes
  ansible.builtin.command: mount -t nfs -o "{{ nfs_mount_opts }}" "{{ master_private_ip }}:/data" /data
  when: is_not_master_host
